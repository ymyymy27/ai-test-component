{
  "name": "@aitest/panel",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "npm run check && esbuild src/index.ts --bundle --format=iife --outfile=dist/index.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
