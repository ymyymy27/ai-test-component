# B-C契约分支待办事项

版本：1.0  
日期：2026-09-25  
负责人：赵  
状态：待B包将prepared_run.py合入develop后执行  
目标分支建议：feature/contract-b-c-preparedrun-execution-facts  
目标PR：develop  
关联文档：docs/接口对接/B-C-PreparedRun与词汇表合同.md

## 1 前置条件

- [ ] B包已将src/aitest/contracts/prepared_run.py合入develop。
- [ ] B包已将RunTierFact、RunDriverFact和ConclusionCeilingFact及对应漂移测试合入develop。
- [ ] 已创建对接Issue，建议标题为“[对接] B-C PreparedRun与ExecutionFacts合同对齐”。
- [ ] 已从最新develop切出独立契约分支，不直接在feat/package-c-execution上修改。
- [ ] 已确认Reviewer包含B包负责人和C包负责人。

## 2 ExecutionFacts代码修改

### C-01 conclusion_ceiling取值

- [ ] 将success、failure、unknown三份ExecutionFacts夹具中的conclusion_ceiling从full改为passable。
- [ ] 保持三份夹具的tier为full。
- [ ] 不把档位和结论上限合并为同一字段。

### C-02 和 C-03 使用B包合同枚举

- [ ] 从aitest.contracts.prepared_run导入ConclusionCeilingFact、RunDriverFact和RunTierFact。
- [ ] RunFact.driver类型改为RunDriverFact。
- [ ] RunFact.conclusion_ceiling类型改为ConclusionCeilingFact。
- [ ] RunFact.tier类型改为B包RunTierFact。
- [ ] C不根据覆盖度或执行方式自行推导conclusion_ceiling。

推荐导入：

    from aitest.contracts.prepared_run import (
        ConclusionCeilingFact,
        RunDriverFact,
        RunTierFact,
    )

### C-04 删除重复RunTierFact

- [ ] 删除execution_facts.py中的本地RunTierFact类。
- [ ] 不在execution_facts.py中重复声明RunDriverFact和ConclusionCeilingFact。
- [ ] 选择方案甲，不新增contracts/run_vocabulary.py。
- [ ] 确认domain/planning与contracts的分层保持不变；B包负责两层值集合一致性测试。

### C-06 intent_id

- [ ] 在RunFact中增加intent_id: str = Field(min_length=1)。
- [ ] 三份现有夹具增加run.intent_id。
- [ ] quick夹具增加run.intent_id。
- [ ] RunFact.intent_id表示产生本次运行的PreparedRun准备意图。
- [ ] AttemptFact.intent_id继续允许为空，用于外部导入或无法证明原意图的材料。
- [ ] 本地正常执行链中，AttemptFact.intent_id应等于所属RunFact.intent_id；后续可增加条件校验。

### C-05 scope语义

- [ ] 为required_scope增加description：PreparedRun.frozen_required_case_ids，即M。
- [ ] 为selected_scope增加description：PreparedRun.selected_case_ids，即S。
- [ ] 不把模板下限T加入ExecutionFacts；T仍属于B内部校验输入。

### C-07 plan_revision一致性

- [ ] 保留ExecutionFacts顶层plan_revision和RunFact.plan_revision，供快照索引和Run事实使用。
- [ ] 增加ExecutionFacts模型校验：self.plan_revision必须等于self.run.plan_revision。
- [ ] 不一致时抛出明确错误，禁止D读到两套计划修订。

### C-08 quick夹具

- [ ] 新增tests/contracts/fixtures/execution_facts/quick.json。
- [ ] tier取quick。
- [ ] conclusion_ceiling取partial。
- [ ] evidence_level为null。
- [ ] 不派生A/B/C/D级证据等级。
- [ ] 更新test_execution_facts.py覆盖quick夹具。

### C-09 漂移检查

- [ ] 删除C侧重复枚举后，由B包的prepared_run合同成为跨包词汇表唯一声明点。
- [ ] 增加或保留结论上限派生测试：quick/on_demand到partial，full到passable。
- [ ] C侧测试导入B包枚举后运行，证明合并顺序正确。

## 3 Schema和夹具同步

- [ ] 修改ExecutionFacts合同后运行scripts/generate_schemas.py。
- [ ] 更新src/aitest/contracts/schemas/ExecutionFacts.json。
- [ ] 更新success.json、failure.json、unknown.json。
- [ ] 新增quick.json。
- [ ] 确认不再存在conclusion_ceiling为full的夹具。
- [ ] 确认所有RunFact夹具都有非空intent_id。
- [ ] 确认runner、spool和假适配器测试没有被合同字段变化破坏。

## 4 本地验证命令

先激活项目Python 3.13.2并确保dev依赖已安装。

    python -m pytest
    python -m ruff check .
    python -m ruff format --check .
    python -m mypy

定向合同验证：

    python -m pytest tests/contracts/test_execution_facts.py
    python -m pytest tests/contracts/test_generated_schemas.py

如B包prepared_run已合入develop，必须确认以下导入成功：

    from aitest.contracts.prepared_run import (
        ConclusionCeilingFact,
        RunDriverFact,
        RunTierFact,
    )

## 5 PR内容

PR标题建议：

    [Contract] B-C PreparedRun与ExecutionFacts合同对齐

PR变更清单：

- 删除C侧重复运行词汇表枚举。
- driver和conclusion_ceiling改为B包合同枚举。
- RunFact增加非空intent_id。
- 修正conclusion_ceiling夹具取值。
- 新增quick档位夹具。
- 补充M/S范围字段说明。
- 增加plan_revision一致性校验。
- 重新生成ExecutionFacts JSON Schema。
- 更新B-C公共对接文档的确认记录。

需确认项：

- conclusion_ceiling只由tier派生。
- RunFact.intent_id为必填。
- RunTierFact、RunDriverFact和ConclusionCeilingFact只在B合同声明。
- quick夹具不包含evidence_level。
- top-level plan_revision与RunFact.plan_revision必须一致。

## 6 禁止事项

- [ ] 不直接在feat/package-c-execution修改正式合同。
- [ ] 不在B包prepared_run.py未合入develop前让C代码硬依赖该模块。
- [ ] 不在C侧重新复制B包枚举。
- [ ] 不由C根据档位或覆盖度重新计算conclusion_ceiling。
- [ ] 不只修改代码而不更新公共接口文档、Schema、夹具和测试。
- [ ] 不直接修改main或develop。
- [ ] 不在没有PR确认的情况下合并正式契约。
