# 一期功能文档：Trae 本地测试闭环

版本：2.6  
更新日期：2026年9月21日  
状态：修订基线，待实现与实测；文档不代表产品已通过验收。

[需求与验收正文](../需求文档/01-需求文档.md) · [总体架构](../../总体架构.md) · [面板设计](../设计文档/01-面板与工作台.md)

## 1 功能及入口

一期17项功能，编号P1-FR01—P1-FR17；验收P1-AC01—P1-AC35。纯本地记录，不登录平台、不上传、不监听TCP。被测Git与普通目录、单模块与多模块都可完成适用检查。

| 功能 | 输入与操作 | 输出、异常与入口 |
| --- | --- | --- |
| P1-FR01 项目与上下文 | 绑定规范目录，登记职责、模块、接口、依赖和环境；源码固定 | 稳定项目/绑定修订；plain不渲染仓库字段；目录移动需重确认；首次向导/项目详情 |
| P1-FR02 标准交付 | 任务、验收项、完成/未完成、运行方式、数据、Mock、自测证据；人工或AI草稿 | 用户确认提测，开发自述与测试验证分开；缺必填阻塞；任务与交付子视图 |
| P1-FR03 变更回归 | 需求/接口/代码/规则变更，按用例↔模块和反向依赖匹配 | 使用者→提供者，改变提供者反向传播；缺映射标可能不完整；单模块仅跨模块传播不适用，本模块变化仍用于快速检查 |
| P1-FR04 模板与草稿 | 版本模板、实际上下文生成检查内容；确认发布 | 模板必须给必测/关键链路/适用条件；缺清单为不完整，明确无适用项才可空集合；人工修订不被生成覆盖 |
| P1-FR05 规则版本 | 人类步骤、AI规则、证据和输出规范；Markdown导入导出 | 发布不可变，新测试用新修订；未发布不生效，正文不触发工具；规则编辑子视图 |
| P1-FR06 计划用例 | 验收项、层级、前置、输入、步骤、预期、Mock、重要性、独立核验、断言依据 | 草稿/发布独立；正常、边界、异常、依赖失败、会话差异按适用性覆盖；关联模块/关键链路/环境 |
| P1-FR07 档位驱动 | 选档位、驱动，发布计划后准备意图 | 冻结范围/源码/环境/规则；quick和on_demand不打分、不完整通过；full完整门禁；运行中修订按§4 |
| P1-FR08 源码可运行L1 | Python语法、适用构建、加载或最小启动；其他栈标准导入 | 原始输出/退出/定位；源码、依赖、环境、工具失败分开，无成功证据不称可运行 |
| P1-FR09 工作台 | 自动或人工逐步执行，暂停/恢复/取消/重试/补证/退回 | 原始事实、人工操作与AI分析分开；新尝试立即当前，取消未确认保持待核实 |
| P1-FR10 模块L2 | 项目既有测试入口与参数，断言和Mock范围 | 命令/输出/日志/独立核验；局部Mock通过不等于真实集成通过 |
| P1-FR11 接口集成L2 | 串联请求、前步变量、认证引用、业务断言、独立核验 | 成功状态不代表业务正确；核对同业务对象持久化及目标部署版本；无核验能力未完成 |
| P1-FR12 Mock真实性 | 开发声明、配置、静态候选与运行证据分别登记 | 真实/Mock/混合/未知；静态命中只作疑点，不用关键词证明真假 |
| P1-FR13 业务L3 | 人工页面或接口/命令/智能体连续业务目标 | 同一对象、完整步骤、最终核验；页面按钮/分页/跳转/表单/滚动/适用鼠标/异常提示均有证据，不自动录制回放 |
| P1-FR14 报告导出 | 冻结当前尝试、证据、问题、策略与范围，Markdown摘要/证据包导出 | 正文不可变；本地复核独立；变化生成新报告修订；导出带明确复核集合且脱敏 |
| P1-FR15 缺陷回归 | 统一问题编号，确认级别/责任人、登记修复版本、回归 | 产品/环境/数据/脚本原因分开；关闭必须新实际证据；复测失败回处理中 |
| P1-FR16 永久保留 | 留存记录、证据、报告、导出、诊断、本地复核 | 不设保留期、不自动删除、不提供删除入口；空间与外部损坏缺口可查；临时物化/冗余副本按既定条件回收 |
| P1-FR17 检查面板 | 当前检查三段、动作区、计划/工作台/设置跳转 | 信息区只读；业务结论与证据等级分列；一期缺口跳记录详情，不依赖二期图 |

## 2 档位、确认与证据语义

| 档位 | 范围与结论 |
| --- | --- |
| quick | 适用L1＋改动模块关键L2＋适用独立核验；由已发布模板与变化匹配确定，不勾选；partial且未打分 |
| on_demand | 用户选层级/模块/环节，未选记未执行而非不适用；partial且未打分，即使全选也不变 |
| full | 开始时覆盖全部适用必测与关键链路；passable但实际通过须满足§3。运行中失败/跳过/取消不改档，只影响真实结论 |

计划驱动默认，按已授权计划连续推进；逐步驱动在边界确认，可合并同类无副作用步骤。有副作用、新凭据或范围扩大必须单独确认。允许planned→stepwise，拒绝反向扩大授权；原始驱动与切换记录都保存。

三类确认不可混用：计划确认绑定Plan修订；执行授权绑定实际动作和输入；结果复核绑定已有Attempt/报告修订。计划确认不确认未来执行结果，也不自动设置人工复核。

断言依据分missing/present_unconfirmed/confirmed。missing阻止进入full必测；present_unconfirmed可运行，计入分母与未核实，不计已验证并降低证据等级。依据确认和实际独立核验各自记录。未发布用例不执行，不等于已发布但依据未确认。

## 3 判定与统计唯一口径

业务结论为passed/failed/incomplete/not_applicable，按本轮检查集合S评价；S及完整验收必测集合M的定义见下表。S中存在有效失败则failed并同时列未完成项；不能只检查必测项而隐藏本轮所选补充项的失败。没有失败但S尚未全部有效通过或关联有效问题阻塞时为incomplete。S全部有效通过且无关联有效阻塞时，quick/on_demand显示“所选检查通过”，结论上限仍为partial、等级仍为空；full还须满足完整门禁。未选范围另列未执行，不拿来把所选检查改判为不适用。S为空且有明确不适用依据才显示not_applicable；用户未选择任何检查则阻止启动，不能生成不适用结果。

完整通过同时要求：full档、模板必测集合包含、全部本轮检查有效通过（S⊆P，含明确增补项）、适用必测全部纳入（M⊆S）、跳过为零、无待核实/过期依据、所需真实链路有证据、源码与执行对应、无关联有效P0/P1阻塞问题（按第12.2节处置规则）。低等级本身不改业务结论；依赖环境方式不直接决定等级或通过。环境与数据问题按具体无效证据或验收失败判定；证据完整的非虚拟环境可得A，venv也不保证证据齐全。未确认依据不能计已验证，完整通过还须确认所有适用必测项的断言依据，并有满足原验收条件的实际核验证据；补充确认不替代实际核验。

仅full计算证据等级，取命中条件最严重者；同严重度按下表顺序给主要缺口，全部缺口保留：

| 等级 | 显式条件 | 不表示什么 |
| --- | --- | --- |
| D 无有效依据 | 源码身份与执行不一致；或整轮无任何有效执行依据（含经核实可复用的来源证据） | 不把可信的业务失败证据作废 |
| C 关键证据不足 | 所需源码无法核实；关键链路缺失/Mock/未知；必需证据缺失/损坏；必测阻塞、取消、未执行且无有效复用或待核实；相关依据过期；环境问题导致必需核验缺失/失效；实际必测集合低于下限 | 不由字母替代真实业务结论 |
| B 局部不足 | 没有D/C，存在非关键局部未知、非关键核验缺口或已填未确认依据 | 不表示可提测/可放行 |
| A 证据充分 | 无上述缺口且所有适用判定有完整可追溯证据、关键真实路径已核验、依据确认、源码对应 | 可以有充分证据证明业务失败；A不等于通过 |

P0/P1控制业务放行，不把证据自动降为D。无法归类的缺口保守给C并说明原因，不进入默认A。A主要缺口为“无”。快查/按需等级为null，显示未打分和相同事实缺口。

执行完成、有效复用、有效验证分别计数，统计单位为用例；自动步骤进度独立计算。集合随精确来源修订派生，不能只用数字相加猜测覆盖。

| 集合 | 唯一定义 |
| --- | --- |
| M | 已发布AcceptanceScope内全部适用必测用例ID，不随quick/on_demand选择而删减 |
| T | 模板适用条目经冻结CaseLink映射的必测用例ID；每个条目须有完整映射，full须T⊆M |
| S | 本轮纳入的适用检查用例ID；quick由规则生成，on_demand为用户确认的选择，full必须包含M并可明确增补；跳过/阻塞不从S移除 |
| E | S中本轮实际执行的当前尝试已完成且输出已保存者；依据未确认仍可计执行完成；取消/执行异常/阻塞不计完成 |
| R | S中本轮未新执行、明确采纳历史来源且复用守卫通过的有效验证用例；必须有精确来源引用，不创建虚假Attempt |
| V | S中依据已确认、来源有效、必需证据/独立核验齐全并有明确通过或失败结论者；来自本轮E或合法R |
| P/F/U | P为V中通过者，F=V−P为有效失败者，U=S−V为本轮未验证者 |

E∩R=∅，R⊆V⊆E∪R⊆S。M与S是不同维度：full有M⊆S，quick/on_demand可只取部分必测或只选补充项。M−S是本轮未选的必测范围，保留未执行原因，不并入S的进度分母，不标成不适用。用户增补项一旦纳入S，其失败也进入本轮结果。

| 统计 | 公式与呈现 |
| --- | --- |
| 本轮执行完成 | ｜E｜/｜S｜，文字明确是本轮实际新执行 |
| 有效复用 | ｜R｜/｜S｜，独立列数和来源，不标为本轮已执行 |
| 本轮有效验证 | ｜V｜/｜S｜，含有效失败与有效复用；未验证为｜U｜ |
| 本轮用例通过 | ｜P｜/｜V｜，同时展示失败与未验证，不代替完整通过 |
| 完整验收必测验证覆盖 | ｜V∩M｜/｜M｜，仅反映本轮覆盖；不自动拼入未选历史绿灯 |
| 模板纳入覆盖 | ｜S∩T｜/｜T｜，full另校验T⊆M⊆S及逐条映射；补充项不能抵漏项 |
| 关键链路覆盖 | 已有满足要求的有效核验条目/已发布范围适用关键链路；本轮未覆盖仍列缺口 |
| 断言依据确认 | 已确认依据的本轮参与用例/本轮参与用例；不是独立核验完成率 |

空分母显示“本轮无检查”“无适用必测项”或“暂无有效验证”等对应原因，不显示100%。非必测补充项仍可属于S；M为空不抹掉S的执行与业务结果，full仍须模板适用性依据，不能拿空集合规避必测。

复用须满足项目与计划分册的源码、环境动态状态、入口参数、范围、规则/断言及适配器版本一致性，证据可读、依据已确认，并且没有必须新执行的要求。合法R可支持full的有效验证和证据等级，不因“本轮未新执行”自动降C；必须真实的链路仍需原证据真实且当前条件可证明有效。缺陷修复关闭、发布规则指定的新执行或动态状态无法证明未变时，不得进入R。来源失败可作为有效失败复用，不能只复用绿灯。

本轮新Attempt一旦建立，即撤销同用例当前复用选择；新尝试失败、取消、未知都不得回退R中的旧通过。复用证据损坏、来源/环境变化时R和V随当前有效性减少，历史报告不改。复用核实动作不是被测业务的新执行，不能用它生成已执行Attempt。

| 例子 | 一致呈现 |
| --- | --- |
| full，S=M=12项，全部新执行，2项依据未确认，其余10项通过 | 执行12/12、复用0、验证10/12、未验证2；未完成 |
| full，S=M=12项，新执行10项、合法复用2项，全部有效通过 | 执行10/12、复用2/12、验证12/12；其余门禁满足才完整通过，不显示本轮执行12项 |
| on_demand，M=12项，只选3项非必测补充用例且通过 | 执行/验证均3/3、所选检查通过、未打分；必测验证覆盖0/12，12项未选仍可查 |
| on_demand选3项，其中1项有效失败 | 本轮失败，完整验收仍未进行；不能因失败项非必测而忽略 |

L1/L2/L3分层统计，上层失败只阻塞依赖它的步骤；独立用例可继续。无前端仅网页操作不适用，仍应通过接口/命令完成适用L3。GitHub绿色汇总不替代原始L1证据。

## 4 状态、修订与幂等

执行状态：未执行/排队/执行中/暂停中/已暂停/取消中/待核实/已完成/阻塞/已取消/执行异常。断言结果：未判定/通过/失败/不适用。依赖真实性：真实/Mock/混合/未知。人工复核与计划确认分别存储，不覆盖其他维度。

待核实为非终态，适用于取消停止未确认或副作用结果未知；只有实际核实/补证后离开，不自动重放。业务重新执行新建Attempt（同一请求传输恢复除外），尝试序号从1起，重试数=尝试数减1；新尝试立即当前，不回退旧通过。

只在步骤边界修改未执行内容；正在执行的步骤拒绝修改。full不可删必测或弱化要求，违反时拒绝或新建运行。需求/规则/输入不兼容变更须新修订，已完成事实不回改，受影响运行暂停；继续执行不意味着可用过期依据通过。退回阶段标记下游需重跑，旧证据保留并标来源，不伪造已重跑。

prepare_run冻结实际输入并产生intent_id；start同intent原子取得既有run或建立一个run。同intent异payload_hash冲突。重跑显式准备新意图，跨入口恢复同一意图不重跑。start物化摘要须匹配prepare冻结源码；变化时旧意图不可启动，重新准备并核对授权。导出键包含报告修订、所带复核集合、格式和脱敏策略；不得只用计划号去重。

## 5 记录与来源

记录均带schema_version、项目、稳定编号、修订、来源、本机采集时间；业务顺序以提交序号判断，不按可调整的系统时间选最新。关键实体：LocalProject、LocalProjectBinding、Module、Dependency、Task、AcceptanceItem、Delivery、TemplatePack、RuleVersion、Case/CaseLink、Plan、Run、Step、Attempt、SourceSnapshot、EnvironmentRef、EvidenceRef、TraceNode、MockDeclaration、Verification、IssueRecord、ReportSnapshot、LocalReview、ReportExport。

用例链接验收项、模块、环境和关键链路；步骤绑定执行时计划修订；证据绑定运行/步骤/尝试、源码身份及采集来源；报告引用精确记录修订及判定策略。问题与缺陷单是同一issue_id，不另建AI副本。

生成缓存按实际依赖修订集合与生成策略版本，依赖变化定向失效。模型只输出草稿/假设，保留输入来源、模型配置修订、供应方请求标识（若有）、原始响应与错误；迟到响应核对来源修订。模型不可用时原证据、人工处理和导出继续。

其他技术栈按aitest.external-result/1.0导入，保留EXTERNAL_IMPORT；来源号同内容幂等、异内容冲突。能独立核验的重算，不能将外部自述转成真实。被测智能体只认实际采集工具调用；随机业务输出按发布规则逐次评价，默认重复三次属于测试设计，各次保留结果。

## 6 环境、真实性与L3

默认虚拟环境，显式不隔离允许且冻结，环境条件独立展示、不自动降证据等级；源码仍复制到运行目录。网络目标与凭据分别支持模型、被测HTTP、独立核验数据库、可选GitHub只读。数据来源/隔离/复位、业务对象ID、目标部署身份须可追溯；本地源码物化不证明远端版本一致。

请求ID、实际trace父子关系或明确业务关联才形成调用边；独立核验单列，不能拼成原应用调用。无请求标识/结构化日志时人工关联保留级别与缺口，不因缺采集工具直接称产品缺陷，也不因此通过。最终一致性核验使用配置的查询/截止规则，截止只表未知或核验失败，不表示事务回滚。

工单试点：创建并保存ID→查询分页找到同ID→详情一致→修改状态→刷新/重新进入并独立核验最终持久化。任一必需环节失败、跳过或关键依赖未知不能整流程通过；异常路径独立验收。

## 7 问题、报告与留存

问题字段：标题、级别、影响范围、验收项、用例/步骤/尝试、版本与环境、复现步骤、预期/实际、频率、证据、事实/假设、责任人、修复/回归与关闭依据。P0核心全面不可用或严重数据损坏；P1关键业务/结果错误；P2局部影响有替代；P3轻微体验。经确认的有效P0/P1问题按第12.2节阻止关联范围通过，暂缓不得绕过；草稿候选与已核实处置分开。

状态：草稿→待确认→已确认→处理中→待复测→已关闭；复测失败回处理中；重复/不属于缺陷/暂缓保留原因，不删除。修复关闭读取新的实际执行证据，不接受AI或修复说明替代；非缺陷与重复归并按第12.2节独立处置，不伪装为修复通过。

当前结论键=项目＋源码身份（Git基准与diff/非Git文件清单及排除规则）＋适用范围＋环境＋规则/验收/模板/判定策略修订。新full开始即标重新验证，取消/未知不回退旧通过；旧结果只作历史。快查/按需只补充。

报告正文和等级随精确输入修订冻结，变化生成新内容修订；LocalReview独立引用正文，不修改事实/正文。导出冻结所附复核集合并使用脱敏投影；摘要明确不附带证据；证据包含report.md、manifest.json及脱敏附件，相对路径在新目录可访问，缺失/排除逐项列明，不依赖原工作空间链接。缺证据时保留旧历史结论并标当前有效性缺口，不篡改旧正文。

全部业务记录、证据、报告修订、导出、诊断与复核永久保存，无保留期、无自动删除、无删除入口。只回收证据已落盘且执行停止后的运行副本/运行临时文件/上传冗余临时副本，以及经恢复确认的空对象和无有效内容的事务残留；已写入输出先抢救为部分或完整证据，不能先清空。空间统计不删除材料。

## 8 验收对应

AC场景和预期仅由需求文档维护。P1-FR01—07主要对应P1-AC01/03/12/17—25；P1-FR08—13对应P1-AC02—10/13/15/18—25；P1-FR14—15对应问题、报告与回归场景；P1-FR16对应P1-AC28；P1-FR17对应P1-AC26/27；独立本地复核为P1-AC29；2.2新增产品流程对应P1-AC30—35。完整逐条映射附后。

## 9 常用流程与增量合同

最小输入、分析时点、验收范围和模型出站以需求§7为产品合同。首次建立单模块草稿和适用计划摘要；快速/按需只校验所选检查所需信息，正式提测才要求交付表，full才要求对应范围全部验收条件。必需检查尚不支持不等于不适用。执行能力和内置模板见[能力与模板清单](02-能力与模板清单.md)。

显式analyze_project创建analysis用途SourceSnapshot，包含所选路径、排除项和内容摘要；GeneratedContent绑定其修订，不能作为已执行证据。计划发布时核对相关来源变化，启动仍物化并核对准备意图。无模型也能人工建立计划，不自动推断可信断言。

AcceptanceScope属于计划的不可变修订数据，含目标、包含/排除范围及原因、依赖闭包和模板适用性。运行冻结scope_revision，结论键的scope取该身份；标题固定“范围名称＋本轮结果”。范围变更重新发布，不得追认原运行覆盖新范围。

ActionAuthorization按动作逐项绑定intent_id/计划步骤、input_digest、target、credential_scope和plan_revision；已解析输入可提前确认，未解析值待执行前确认；start创建Run后关联原intent授权；记录清单式逐项确认，不做集合授权。重复传输不重复询问，但副作用新Attempt需新授权；执行前再核对实际输入。

ModelOutboundPolicy按项目修订，保存接收地址/供应方、允许类别、source_snippets=false默认值及确认记录。发送前核对冻结投影、当前开关与策略修订；扩大数据范围或换目标重新确认，缩小范围立即生效，不再发送被撤销类别。源码片段开关作用所有出站字段及日志/附件投影，不能通过另一类别夹带未授权源码；无法安全投影则排除并显示分析缺口。平台上传的源码禁传规则与此独立。

CopyRepairBrief从Issue/Attempt精确修订生成脱敏文本；事实和假设分段，位置未知不猜测，附复测入口。此动作不执行修复、不关闭问题、不保证可调用Trae聊天API。

查询按项目/状态/编号分页，报告修订折叠；ProjectViewPreference只记录隐藏/恢复偏好，隐藏不停止运行、不删业务数据。当前检查按原因选择下一步：断言失败到问题证据、环境阻塞到对应配置、未确认依据到依据核对、未知副作用到核实；详情继续展示全部缺口。按需和quick仍不显示完整通过。

## 10 逐功能操作合同

本节把第1节总览展开为输入、操作、输出、规则、异常与验收索引。跨功能判定仅由第2—7节定义；以下不建立第二套判定。
### 10.1 P1-FR01 本地项目与模块上下文

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 项目目标、选定资料、目录、模块/接口/依赖、环境及源码版本；Git仓库为可选绑定形态。 |
| 主要操作 | 建立本地项目并生成稳定的本地项目编号；选择本地项目根目录与 Git 仓库并绑定；登记模块职责与依赖；读取选定资料，确认 AI 摘要及推断关系。 |
| 输出 | 模块清单、依赖关系、项目上下文，以及本地项目、仓库与目录的绑定；打开任务时展示本次变化与未完成内容。 |
| 业务规则 | 项目ID稳定，规范路径是可修订的绑定属性而非业务主键。Git固定基准、未提交新增/修改/删除及排除规则；plain固定文件清单与必要内容，不显示仓库字段。显式分析、计划确认与运行准备读取所选源码，普通面板查询不扫描；启动按实际物化字节核对。 |
| 异常处理 | 目录移动或绑定形态变化需新修订重确认；上下文缺失保持可见。同步/网络盘不作为活动数据工作空间；普通源码目录非Git不阻塞。未保存缓冲区先保存或明确选择磁盘版本。 |
| 验收要点 | P1-AC03、P1-AC25、P1-AC30；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.2 P1-FR02 标准交付说明

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 原任务、完成／未完成项、变更模块、接口变化、运行方法、数据、依赖、Mock、已知问题和自测证据。 |
| 主要操作 | 按模板填写或由 AI 生成草稿，用户确认后提测；可引用 P1-FR04 的检查内容草稿。 |
| 输出 | 与任务和源码版本关联的交付说明。 |
| 业务规则 | 正式提测需用户确认交付；开发自述完成、实际测试完成和未验证范围分别展示。快速/按需检查按所选能力校验必需输入，不强制填满正式交付表。Mock声明关联依赖，不能直接证明真实性。 |
| 异常处理 | 正式提测缺必填字段或可识别版本时提示具体字段；AI草稿不作完成证据。已确认交付变化形成新修订，不覆盖历史。 |
| 验收要点 | P1-AC01；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.3 P1-FR03 变更影响与回归建议

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 需求、代码、接口或规则变化，已登记模块依赖和用例关联。 |
| 主要操作 | 人工标记变化或按依赖匹配受影响用例；查看 AI 辅助建议，调整并确认回归范围。 |
| 输出 | 待回归用例及其变更依据、人工增加／排除理由。 |
| 业务规则 | 使用者→提供者，沿反向依赖匹配用例；输出来源修订、变更原因和人工增减理由。单模块仍识别本模块变化与回归，仅跨模块传播不适用。无映射标可能不完整，首次quick使用已发布保守集合或等待确认。 |
| 异常处理 | 循环传播去重，删除/重命名保留稳定模块关系；缺用例关联不等于无影响。人工排除须说明，full适用必测不可通过排除隐藏失败。 |
| 验收要点 | P1-AC03、P1-AC12、P1-AC20；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.4 P1-FR04 内置模板与检查内容生成

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 随插件提供的通用模板包（按业务类型与技术栈组织）、项目上下文、模块与依赖、测试规则。 |
| 主要操作 | 选取最接近的内置通用模板；由 AI 结合项目上下文生成检查内容草稿，包括规则条目、验收项、用例和检查项；用户增删改后确认发布。 |
| 输出 | 模板版本、**最小必测项清单与关键链路清单**、生成内容及其来源标记、确认状态，以及发布后的规则版本和测试计划。 |
| 业务规则 | 模板版本冻结适用条件、必测ID和关键链路要求；缺少清单为模板不完整，明确无适用项才用有依据的空集合。生成只写草稿，不能增加工具或执行入口；人工改动保留。按实际来源修订集合复用，相关变化才定向失效。 |
| 异常处理 | 无匹配模板转人工/导入；缺上下文列出缺口。模型失败保留已有草稿，迟到结果只附原来源；未授权出站材料排除并标分析缺口。 |
| 验收要点 | P1-AC17、P1-AC24、P1-AC30、P1-AC31、P1-AC32；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.5 P1-FR05 测试规范与规则版本

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 面向人的步骤说明、AI 检查规则、证据要求和输出格式。 |
| 主要操作 | 分类、修订、绑定本地项目；导入／导出 Markdown；确认后发布规则，并按本册第11节执行模块流程。 |
| 输出 | 不可混用的规则版本、统一模块流程和标准输出要求。 |
| 业务规则 | 正文说明、结构化规则和发布状态分开。新测试明确绑定新规则修订，旧运行仍引用原版；导入只创建草稿，不执行其中命令。往返保留编号、来源与未识别扩展字段，未知执行字段不得静默启用。 |
| 异常处理 | 导入规则未确认发布不生效；文档正文不能自动触发新工具或补造上下文。 |
| 验收要点 | P1-AC12、P1-AC17；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.6 P1-FR06 测试计划与用例生成

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 任务、验收项、交付说明、项目上下文、测试规则及 P1-FR04 生成并已确认的检查内容。 |
| 主要操作 | 按业务分类起草正常、边界、异常、依赖失败与会话差异用例，补齐预期与依据；关联验收/模块/环境/关键链路后确认发布；后续编辑遵守运行修订边界。 |
| 输出 | 关联验收项的用例：层级、前置条件、输入、步骤、预期、验证方法、允许的 Mock、**断言依据**和重要性；编辑历史保留为修订。 |
| 业务规则 | 用例关联验收项、模块、环境及适用关键链路，记录独立核验方式。依据missing不能进入full必测；present_unconfirmed可执行但不计已验证；confirmed仍须实际证据。AcceptanceScope发布前定义，运行中不可删必测或弱化要求；已执行步骤不回改。 |
| 异常处理 | 无预期/前置/必要核验方式不能发布为完整可验收用例；正在执行步骤拒绝编辑。修改使已完成依据过期时暂停提示，新执行前重新核对授权。 |
| 验收要点 | P1-AC01、P1-AC17、P1-AC20、P1-AC21、P1-AC24、P1-AC30、P1-AC31、P1-AC32；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.7 P1-FR07 运行档位与驱动配置

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 已发布计划、AcceptanceScope、模板/规则/环境修订、选定档位与初始驱动；草稿可准备内容，但不能直接执行。 |
| 主要操作 | 先确认验收范围及计划，再选quick/on_demand/full与planned/stepwise；prepare固定意图，逐项授权已解析动作后start；未解析动作执行前确认。 |
| 输出 | 冻结档位、initial/effective_driver、验收范围、结论上限、未执行项、基础计划及运行中修订；intent_id、Run和逐项授权关联。 |
| 业务规则 | 档位决定结论上限，驱动决定推进方式。full覆盖已发布范围全部适用必测；quick/on_demand不打分且不完整通过。计划确认、逐项执行授权、结果复核独立；动作绑定已解析输入与目标，未知输入待确认。仅planned→stepwise；依赖环境方式不自动决定等级。 |
| 异常处理 | 未发布计划或必需配置缺失给明确补齐入口；启动物化与准备摘要不符则旧意图不可执行，重新准备。运行中取消/失败保留原full档位并报告真实未完成/失败，不转换档位美化结果。 |
| 验收要点 | P1-AC18、P1-AC19、P1-AC20、P1-AC21、P1-AC22、P1-AC23、P1-AC26、P1-AC31；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.8 P1-FR08 源码可运行性检查（L1）

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 技术栈、登记入口、指定源码快照、运行依赖和测试环境。 |
| 主要操作 | 执行语法检查、适用构建／编译、必要模块加载及最小启动；纯库或命令工具检查可调用入口。 |
| 输出 | 每项检查的快照、范围、输入、原始日志、退出状态和有依据的错误位置。 |
| 业务规则 | 自动适配范围以能力清单为准；外部结果保留导入来源。源码可运行与业务断言分别判定。复用须源码、环境动态状态、范围、入口参数、规则及适配器版本一致，且证据可读；GitHub绿色汇总不能替代。 |
| 异常处理 | 区分源码错误、依赖缺失、环境不可达、工具失败和通过；不适用子项给出原因，无成功证据不能声称可运行。 |
| 验收要点 | P1-AC04、P1-AC13、P1-AC18、P1-AC23、P1-AC25、P1-AC30；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.9 P1-FR09 引导式测试工作台

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 已确认计划、每步目的、规则、输入、执行方式及预期结果。 |
| 主要操作 | 逐步查看与执行；记录人工操作、工具结果和 AI 分析；暂停、取消、重试、补证或退回阶段。 |
| 输出 | 步骤进度、实际操作、原始证据、结果与下一步建议。 |
| 业务规则 | 步骤按实际计划修订执行；运行状态、断言、真实性、复核分列。重试创建新Attempt并立即当前，副作用新尝试重新授权；未核实停止保持取消中/待核实。补证关联准确尝试，不能把文本确认当成功。 |
| 异常处理 | 环境、凭据、数据缺失阻塞对应步骤；停止结果未知先核实，不能直接重放。模型失败仍可人工处理；写入失败不得显示已保存或已完成。 |
| 验收要点 | P1-AC09、P1-AC10、P1-AC13、P1-AC15、P1-AC19、P1-AC33；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.10 P1-FR10 函数与模块测试（L2）

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 项目既有测试命令／入口、输入、断言、上下游及允许的 Mock。 |
| 主要操作 | 运行函数／模块测试；AI 检查测试逻辑、建议补充用例并解释失败。 |
| 输出 | 命令或入口、输入输出、断言、退出状态、日志及模拟依赖。 |
| 业务规则 | 按登记入口和参数数组执行，自动范围以能力清单为准；其余技术栈按标准结果导入。Mock只支持声明的局部结论，成功退出/非空返回不替代具体内容断言；真实集成另留证据。 |
| 异常处理 | 成功提示或非空返回不能替代内容检查；真实依赖未接通时保留未验证范围。 |
| 验收要点 | P1-AC04、P1-AC05、P1-AC06、P1-AC23、P1-AC25；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.11 P1-FR11 接口与集成测试（L2）

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 接口、环境参数、凭据引用、请求数据、响应断言和核验方式。 |
| 主要操作 | 按业务顺序串联请求，提取上一步输出作为下步输入；核对响应、字段、业务规则、模块交互及适用的数据结果。 |
| 输出 | 各请求输入输出、业务断言、独立数据核验和集成结果。 |
| 业务规则 | 请求关联实际业务对象，响应断言与独立只读核验分开；远端部署身份须能与被测源码对应。最终一致性按预先配置核验条件处理。被测智能体记录实际调用，按已发布业务规则逐次评价，不用插件AI分析替代。 |
| 异常处理 | 响应成功但字段错误或数据未写入时，业务断言失败；缺独立核验能力时显示未完成。 |
| 验收要点 | P1-AC02、P1-AC04、P1-AC07、P1-AC09、P1-AC24；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.12 P1-FR12 Mock 声明与真实性核验

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 开发声明、运行配置、静态候选、依赖清单及实际运行证据。 |
| 主要操作 | 登记允许的 Mock；核对固定返回、内存替代持久化、测试替身、模拟接口和未接通依赖。 |
| 输出 | 绑定源码与环境的真实、Mock、混合或未知状态，以及证据／疑点。 |
| 业务规则 | 开发声明、静态候选、配置与本次实际证据独立保存；不存在mock关键词不能证明真实，出现关键词不能直接证明造假。关键链路要求真实时，Mock/未知不能由普通成功响应抵消。 |
| 异常处理 | 未声明 Mock、关键节点真实性未知或缺真实证据时，不给该范围真实链路通过，提示补证或确认范围。 |
| 验收要点 | P1-AC05、P1-AC06、P1-AC22；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.13 P1-FR13 用户操作与业务场景测试（L3）

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 用户业务目标、操作清单、测试数据、预期结果和证据要求。 |
| 主要操作 | 人工按步骤操作并关联证据；覆盖按钮、分页、跳转、表单、滚动、适用鼠标操作、异常提示和展示数据。 |
| 输出 | 完整 L3 步骤、同一业务对象编号、截图或入口证据、最终数据核验及异常。 |
| 业务规则 | 每个纳入范围的业务目标有完整适用L3。人工页面记录保留来源，接口/命令/智能体可承担无前端业务流程；不要求自动录制回放。各步绑定同一业务对象并独立核对最终结果，异常路径按各自验收检查。 |
| 异常处理 | 必需步骤失败或跳过、关键依赖未知时不能全流程通过；操作正常但展示错误仍可建立缺陷。异常路径另按验收项验证。 |
| 验收要点 | P1-AC08、P1-AC24；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.14 P1-FR14 测试报告与本地导出

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 项目、任务、源码与环境、规则、分层结果、Mock、问题、未验证项及证据。 |
| 主要操作 | 生成指定来源修订报告，登记独立本地复核，预览并导出Markdown摘要或证据包；范围和附件缺口始终可见。 |
| 输出 | 不可变报告、范围与分层统计、业务结论、full证据等级/主要及全部缺口（非full未打分）、独立复核、证据索引与导出清单；记录时间仅作追溯。 |
| 业务规则 | 报告冻结精确来源修订，业务结论、证据等级、环境事实与范围分开；变化生成新报告。独立LocalReview不改正文。导出选择Markdown摘要或可携带证据包，冻结复核集合、附件、策略与实际字节摘要。永久保留不等于外部损坏永远不发生。 |
| 异常处理 | 缺失/损坏附件明确列缺口；导出失败不发布成功记录。不能安全脱敏的附件排除，摘要不冒充完整交付包；报告过期提示新修订，不重写原文。 |
| 验收要点 | P1-AC02、P1-AC07、P1-AC13、P1-AC14、P1-AC16、P1-AC18、P1-AC19、P1-AC20、P1-AC21、P1-AC22、P1-AC23、P1-AC24、P1-AC25、P1-AC27、P1-AC28、P1-AC29、P1-AC31、P1-AC34、P1-AC35；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.15 P1-FR15 缺陷管理与修复回归

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 失败步骤、原始证据、AI 建议、修复版本及复测结果。 |
| 主要操作 | 生成草稿、人工确认、分级、指派本地责任人、补充信息、登记修复、安排回归及核对关闭依据。 |
| 输出 | 每次运行的统一问题清单、问题处理记录及前后版本回归关联。 |
| 业务规则 | 问题、缺陷单和AI建议共用issue_id；事实与假设分开。严重程度及责任人由用户确认，P0/P1只阻止关联范围通过，不把充分失败证据降成D。修复关闭须原标准下新实际执行证据，修复说明和继承证据不替代；核实非缺陷/重复归并按第12.2节另行处置。 |
| 异常处理 | 先区分产品、环境、数据和脚本原因；缺证据显示信息缺口。修复说明或 AI 判断不能直接关闭缺陷。 |
| 验收要点 | P1-AC08、P1-AC10、P1-AC11、P1-AC14、P1-AC15、P1-AC33；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.16 P1-FR16 永久保留与材料维护

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 当前清单（业务记录、证据对象、导出文件、诊断日志、**运行期临时产物清单**）、空间占用统计、外因造成的缺口清单。 |
| 主要操作 | 查看占用、材料与缺口，执行完整备份/迁移，或明确确认已知缺口后按无新增损失守卫迁移；恢复流程先核实活动句柄、抢救输出，再回收确认安全的非业务临时材料。 |
| 输出 | 空间组成、材料/缺口清单、备份迁移结果与安全临时回收记录；不生成业务删除任务。 |
| 业务规则 | 所有业务记录、证据、报告修订、导出、诊断和复核永久保存，无保留期、自动删除或删除入口。只回收停止且证据保存后的运行副本/冗余临时材料，以及恢复确认的空对象或无有效内容事务残留；先抢救有效输出。 |
| 异常处理 | 空间不足停止接纳新增字节并按控制流程暂停/停止；不驱逐历史。回收失败保留待处理记录，不影响永久材料；外部损坏产生可用性缺口。已知缺口不伪装修复，可按存储分册第11节受控迁移；新增损失或未知活动执行仍阻止切换。 |
| 验收要点 | P1-AC28、P1-AC34、P1-AC35；场景与预期见需求文档，检查项见本节输出及异常。 |

### 10.17 P1-FR17 检查信息面板

| 项目 | 功能说明 |
| --- | --- |
| 输入 | 源码/绑定、环境、计划范围、运行/步骤/当前尝试、CoverageDTO/DecisionDTO及报告精确修订；不从页面重新扫描源码或推算结论。 |
| 主要操作 | 从当前检查动作区开始/暂停/恢复/取消/重跑；导航到计划、工作台、报告与问题、设置。信息区只读，业务编辑位于相应子视图。 |
| 输出 | 运行前/运行时/运行后三段信息、可用动作和原因、对应计划/步骤/问题/报告的导航，原始字段在详情中可追溯。 |
| 业务规则 | 当前检查只读信息与动作区分开；主屏先给范围、本轮结果、原因和下一步。统计与等级只读核心DTO，full才有等级。plain不渲染Git字段；一期缺口到记录详情，二期才有源码标注和图。面板当前投影与报告冻结快照分别标明。 |
| 异常处理 | 首次无记录、计划未发布、模型不可用、记录不可写、连接中断分别展示原因与下一步。新full取消/未知不回退旧通过；无模型不阻断可行的本地路径。 |
| 验收要点 | P1-AC26、P1-AC27、P1-AC30、P1-AC32、P1-AC33、P1-AC34；场景与预期见需求文档，检查项见本节输出及异常。 |

## 11 分层执行与业务示例

| 阶段 | 输入与动作 | 必须留下的结果 |
| --- | --- | --- |
| 理解上下文 | 任务、验收、模块输入输出、上下游和版本 | 已知上下文与缺失项，AI推断与已确认关系分开 |
| 准备 | 核对入口、所选源码、运行依赖、测试数据及复位方式 | 可执行条件或具体阻塞；不以准备自检替代L1/L2/L3 |
| 声明依赖 | 对每个相关依赖登记真实目标或允许Mock | 替代对象/行为/范围及尚未验证的真实连接 |
| 选择用例 | 已发布计划、正常/边界/异常/身份/依赖失败场景 | 适用性依据、预期、断言依据与核验方式 |
| 执行 | L1语法/适用构建/加载；L2模块和集成；L3完整业务目标 | 各自原始输出、实际断言、真实性和缺口，失败不抹掉其他层事实 |
| 处理 | 确认问题、登记修复、新执行回归、报告与复核 | 同一问题关联前后版本；旧失败、全部尝试永久保留 |

无需编译时只有该子项不适用，语法/入口仍检查；无前端只免网页交互，不免业务L3。独立用例可在其他层阻塞时继续，但不得因此汇总全范围通过。

### 11.1 链路工作流

准备选定用例、环境、业务对象和Mock→人工操作或HTTP执行→采集真实请求标识与结构化日志→对同一对象独立查询→关联问题→修复后按原标准回归。HTTP执行器只采集自己实际发出的请求；被测服务的内部节点依赖实际提供的日志或trace，不承诺自动插桩。人工关联必须标来源，独立查询不画成原业务调用。

| 工单顺序 | 必须核对 |
| --- | --- |
| 创建 | 保存真实工单ID及关键字段，不能只看成功提示 |
| 查询和分页 | 在适用筛选/分页下找到同ID，检查边界、重复与遗漏 |
| 详情 | 编号、字段与创建/列表一致 |
| 修改状态 | 记录真实操作、请求、权限条件和预期状态 |
| 刷新/重新进入 | 同ID状态仍正确，独立核验最终持久化 |

文件上传/检索示例分别验证实际标识、内容一致性、无匹配和身份异常；无持久化的纯函数用例按独立期望或参考计算核验，不强制数据库。演示既要证明被测业务完成，也要证明插件完成计划→执行→证据→问题→回归→导出的本地闭环，两者不能互相替代。

## 12 标准字段、状态与交接

| 记录 | 必备字段组 | 关联和缺失处理 |
| --- | --- | --- |
| 项目/模块 | 稳定项目ID、目标、工作空间、职责、接口、依赖、实现状态、本地责任人 | 目录/仓库作为绑定修订，本地责任人不是平台身份 |
| 任务/验收 | 目标、业务范围、可观察验收条件、版本、来源、确认状态 | 未关联用例或证据显示待验证 |
| 交付说明 | 完成/未完成、变更模块、运行方式、版本、数据、依赖、Mock、已知问题、自测证据 | 自述不作为已验证数 |
| 用例/步骤 | 层级、前置、输入、操作、预期、依据三态、独立核验、证据要求、允许Mock | 关联验收项/模块/关键链路与执行时修订 |
| 运行/尝试 | 档位、初始/有效驱动、范围、意图、Attempt序号、执行句柄、状态、原始输出、源码/环境 | attempt_index从1起；重试数为尝试数减1，不用旧成功替代新尝试 |
| 证据/链路 | 来源、项目/Run/Step/Attempt、对象摘要/大小、完整性、节点ID/真实父子关系、真实性 | 未采集与缺失分别注明，无法确认位置写未知原因 |
| 环境/源码 | 依赖运行时、业务数据来源/复位、目标部署身份、SecretRef；所选文件清单、排除规则、复取依赖 | 名称相同不证明环境相同，提交号不保证永久可复取 |
| 问题 | issue_id、标题/类型/级别/范围、复现前置和顺序、预期/实际/频率、证据、事实/假设、责任人 | 修复版本、新回归与关闭依据归同一编号，不另建AI问题副本 |
| 报告/导出 | 范围、适用层级、分层结果、未执行/阻塞、Mock/未知、依据过期、证据索引、来源修订 | 报告不可变；本地复核独立；摘要和证据包分别标交付完整性 |

执行状态、断言结果、依赖真实性、人工复核、计划发布确认分别展示。例如“已完成/断言通过/Mock/待复核”仍不证明真实集成通过。计划确认只绑定计划修订，不能确认未来结果；人工复核不覆盖原始断言。

### 12.1 缺陷处理与交接格式

| 阶段 | 操作与必要条件 |
| --- | --- |
| 草稿→待确认 | 失败证据或人工记录，事实与假设分段 |
| 待确认→已确认→处理中 | 用户核实影响、级别与本地责任人，保留确认记录 |
| 处理中→待复测 | 填修复版本、原复现入口与通过标准 |
| 待复测→已关闭 | 新的实际尝试及证据满足原关闭标准；不是重填说明 |
| 复测失败→处理中 | 继续原issue_id，保留前次修复及失败 |
| 重复/非缺陷/暂缓 | 按第12.2节保存独立处置；重复沿主问题判断、非缺陷需核实依据、暂缓仍阻塞 |

P0示例为核心全面不可用或严重数据损坏；P1为关键流程无法完成或假成功但数据未保存；P2为存在替代的局部异常；P3为不影响主要任务的轻微体验。严重程度与开发排期不同，不将P2/P3一概视为满足对应验收项。

复制修复说明与Markdown表单使用下列顺序：来源编号/修订→业务范围→版本/环境/数据→复现步骤→预期/实际→脱敏证据→已观测事实→待核实假设→位置或未知原因→修复版本与复测入口。未确认信息不猜测；非原实现人员凭材料应能复现确定性问题。摘要注明附件未随交付；需带附件时使用证据包及相对路径清单。正文不自动触发修复命令。

### 12.2 问题处置与有效阻塞

IssueRecord的处理进度与disposition分开。disposition为active/fixed/non_defect/duplicate/deferred，保存操作者确认、理由、证据及必要的canonical_issue_id。待确认草稿显示待核实风险，不因AI候选级别直接成为已确认P0/P1；已有真实失败和证据缺口仍按原规则阻止通过。

| 处置 | 必要条件 | 对BlockingRule的影响 |
| --- | --- | --- |
| active | 用户已确认问题与级别，尚未解决 | 关联范围内P0/P1阻塞 |
| fixed | 新实际回归满足原关闭标准 | 修复关闭；保留原失败与修复记录 |
| non_defect | 用户核实误报/脚本原因等，保存具体依据和处置理由 | 该问题不再作为产品缺陷阻塞；不改原执行、断言及证据有效性 |
| duplicate | 指向同项目有效主问题，核实重复依据；禁止自指、循环或悬空引用 | 沿最终主问题计算一次有效阻塞，范围取相关归并范围；不能借归并到低级别问题降低原已确认级别 |
| deferred | 用户记录暂缓原因 | 未解决P0/P1继续阻塞，不等于关闭 |

non_defect停止原修复流程并显示已核实非缺陷；duplicate停止独立修复流程并跟随主问题，原处理进度仅留作历史。撤销上述处置回到待确认并重新核对，不能自动标为修复关闭。

重复链按稳定ID去重解析；主问题仍有效则重复单显示跟随主问题阻塞，主问题修复或经核实非缺陷后不要求每张重复单另跑一次回归。原确认级别需要调整时必须另作有依据的人工确认，不能由归并隐式降级。处置撤销/主问题重开时重新派生当前阻塞，历史报告保持原修订。

误报撤销只改变问题处置；若原用例仍失败或依据失效，报告仍失败/未完成。测试脚本或断言修订后须建立新的有效执行证据才能给新范围通过，不能点击“非缺陷”使原失败变绿。修复关闭与非缺陷结案在界面、报告中分别显示，统一问题编号不删除。

## 13 入口确认与重试边界（2.5）

计划确认、执行授权和结果复核均保存核心确认来源。AI/MCP可准备或请求动作，不能自填“用户已确认”完成发布、授权、依据确认、问题处置、复核或出站策略确认；需要用户决定时返回待确认并指向面板/交互CLI。确认绑定冻结摘要和修订，同一请求重传返回原确认，输入变化须新确认。具体入口矩阵见[Trae接入第9节](../架构文档/05-Trae接入.md)。

重试区分原结果查询、同一业务请求传输恢复和新的业务Attempt。前两者不重新执行业务；同号写请求恢复须有端点保证且保持原输入/目标/幂等键，不能仅凭HTTP方法推断。新副作用Attempt须重新授权；只读新Attempt仍受原允许范围与有界规则约束。传输次数与业务尝试次数分开，细则见[执行与证据第10节](../架构文档/02-执行与证据.md)。

## 附：验收编号映射

预期结果只在需求正文维护。

| 验收 | 关联功能 |
| --- | --- |
| P1-AC01 | P1-FR02、P1-FR06 |
| P1-AC02 | P1-FR11、P1-FR14 |
| P1-AC03 | P1-FR01、P1-FR03 |
| P1-AC04 | P1-FR08、P1-FR10、P1-FR11 |
| P1-AC05 | P1-FR10、P1-FR12 |
| P1-AC06 | P1-FR12、P1-FR10 |
| P1-AC07 | P1-FR11、P1-FR14 |
| P1-AC08 | P1-FR13、P1-FR15 |
| P1-AC09 | P1-FR09、P1-FR11 |
| P1-AC10 | P1-FR09、P1-FR15 |
| P1-AC11 | P1-FR15 |
| P1-AC12 | P1-FR03、P1-FR05 |
| P1-AC13 | P1-FR08、P1-FR09、P1-FR14 |
| P1-AC14 | P1-FR15、P1-FR14 |
| P1-AC15 | P1-FR09、P1-FR15 |
| P1-AC16 | P1-FR14 |
| P1-AC17 | P1-FR04、P1-FR05、P1-FR06 |
| P1-AC18 | P1-FR07、P1-FR08、P1-FR14 |
| P1-AC19 | P1-FR09、P1-FR07、P1-FR14 |
| P1-AC20 | P1-FR06、P1-FR07、P1-FR03、P1-FR14 |
| P1-AC21 | P1-FR07、P1-FR14、P1-FR06 |
| P1-AC22 | P1-FR07、P1-FR14、P1-FR12 |
| P1-AC23 | P1-FR07、P1-FR08、P1-FR10、P1-FR14 |
| P1-AC24 | P1-FR04、P1-FR06、P1-FR11、P1-FR13、P1-FR14 |
| P1-AC25 | P1-FR01、P1-FR08、P1-FR10、P1-FR14 |
| P1-AC26 | P1-FR17、P1-FR07 |
| P1-AC27 | P1-FR17、P1-FR14 |
| P1-AC28 | P1-FR16、P1-FR14 |
| P1-AC29 | P1-FR14 |
| P1-AC30 | P1-FR01、P1-FR04、P1-FR06、P1-FR08、P1-FR17 |
| P1-AC31 | P1-FR04、P1-FR06、P1-FR07、P1-FR14 |
| P1-AC32 | P1-FR04、P1-FR06、P1-FR17 |
| P1-AC33 | P1-FR09、P1-FR15、P1-FR17 |
| P1-AC34 | P1-FR14、P1-FR16、P1-FR17 |
| P1-AC35 | P1-FR14、P1-FR16 |
