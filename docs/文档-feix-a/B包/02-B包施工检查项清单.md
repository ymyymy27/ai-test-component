# B 包施工检查项清单

版本：0.2
日期：2026-09-25
用途：**每次修改 B 包时逐项勾选**，不以代码可导入作为通过依据

依据：一期架构文档《01-项目与计划》；功能文档第 2、3、10.1—10.7、12、15 节；需求 P1-FR01—FR07；`docs/文档-feix-a/B包/00-B包AI规则-AGENTS.md`

---

## 0 当前状态与排期口径

### 0.1 Sprint 完成情况

| Sprint | 内容 | 状态 | 证据 |
| --- | --- | --- | --- |
| 0 | 合同冻结（`PreparedRun`、词汇表、三类夹具） | **已完成** | 提交 `df0f557`、`a96c7c5`、`e8fa690`；`tests/contracts/test_prepared_run.py` 18 项 |
| 1 | 项目上下文领域层 | **已完成**（静态检查、类型检查、全量测试通过；真实环境验收不适用） | `05-项目上下文领域层设计说明.md`；第 11 节证据 |
| 2 | 应用用例与端口接入 | 未开始 | 需 `SourceSnapshot` 归属裁定 |
| 3 | 六个模板从 `scaffold` 升级 | 未开始 | |
| 4 | 规则版本与用例发布 | 未开始 | |
| 5 | 模型出站与可选 GitHub 只读 | 未开始 | |
| 6 | 两次业务交接 | 未开始 | |
| 7 | B 牵头的 8 项 AC 验收 | 未开始 | |

> 本清单第 2—10 节的条目在 Sprint 1 只勾选"项目上下文"相关项；`Case`/`Plan`/`模板`/`规则发布` 相关项在对应 Sprint 勾选。

### 0.2 Sprint 1 排期口径差异（已登记）

`01-B包一期实施计划.md` 第 4 节把"`T/M/S` 校验、`effective_assertion_basis_state` 派生"列入 Sprint 1 完成条件，
但架构文档第 7 节把 `Case`/`Plan`/`AcceptanceScope` 归到 `domain/planning/plans.py`。

**本清单口径**：Sprint 1 = `domain/project/context.py` 全部对象（项目、绑定、模块、依赖、环境、交付、`plain` 内容身份）。
`plans.py` 的扩展随 Sprint 3／4 与模板、用例发布一起做。理由见 `05-项目上下文领域层设计说明.md` 第 1.2 节。

### 0.3 已发现的两份文档偏差（防止据错误描述施工）

| 编号 | 偏差 | 实际情况 |
| --- | --- | --- |
| S-01 | `01-B包一期实施计划.md` 第 3 节称 `domain/planning/plans.py` 现有 `RunMode`/`Driver` | Sprint 0 已改名为 `RunTier`/`RunDriver`；`ConclusionCeiling` 已新增。**以代码为准** |
| S-02 | 同上，第 7 节称"尚未开始编码" | Sprint 0 的合同与词汇表代码已提交（`df0f557`） |
| S-03 | `03-待冻结枚举与跨包字段确认表.md` 第 1、7 节仍称 `ConclusionCeiling` 不存在、`PreparedRun` 不存在 | 两者均已实现；该表第 1、7 节待同步（属文档滞后，非代码缺陷） |
| S-04 | 依赖循环传播去重、驱动单向收窄、档位派生结论上限 | 三者已在 `application/planning/regression.py`、`domain/planning/plans.py` 实现，均有测试；第 2 节原表述近似待办，易导致重复实现 |

---

## 1 开发前

- [x] 已 `git pull origin develop` 并合并到功能分支（项目协作规则第 11 条）
- [x] 未直接修改 `main` 或 `develop`（当前分支 `feat/package-b-project-planning`）
- [x] 已 `git status`，不覆盖他人未提交改动（Sprint 1 开始时工作树干净）
- [x] 已确认本次只修改 B 包目录、合同、测试或文档
- [x] 若涉及跨包字段，已先更新 `docs/接口对接/` 下的文档，未擅自修改 A、C、D 的内部模型
- [x] 已明确本次 FR/AC、输入、输出、异常与恢复分支（FR01/FR02/FR03）
- [x] 已更新本清单或实施计划（项目协作规则第 3 条）
- [x] Sprint 1 设计说明已完成，未编写代码即先行定稿：`05-项目上下文领域层设计说明.md`

## 2 领域模型

> 依据 Sprint 1 的逐项设计与不变量清单：`05-项目上下文领域层设计说明.md`；下表每条与其第 3 节一一对应。

> 以下 25 项在 **Sprint 1** 已完成并验证；验证方式见第 11 节。

- [x] `domain/` 只使用标准库，未 import `pydantic` / `contracts` / `application` / `infrastructure` / `interfaces`
- [x] 领域对象为 `@dataclass(frozen=True, slots=True)`
- [x] 违反不变量抛 `ValueError`（与现有代码风格一致）
- [x] 时间经 `Clock` 端口，未使用 `datetime.now()`；领域对象不含隐式时间默认值
- [x] 业务顺序按**提交序号**（`created_at_commit`）判断，未按系统时间选最新
- [x] `local_project_id` 与对外 `project_id` 是**同一值**：存在权威别名属性并测试 `project.project_id == project.local_project_id`
- [x] 绑定与项目身份分离；目录移动创建**新绑定修订**，历史仍归同一项目（旧修订保留，未被覆盖）
- [x] `binding_form` 决定仓库字段**存在性**：`git` 必须含 `repository_id`/`branch`/`base_commit` 且 `manifest_digest` 为 `None`；`plain` 三者必须为 `None` 且 `manifest_digest` 非空
- [ ] 形态不适用处序列化为 JSON 时真正省略该键（**待 Sprint 2 序列化层**；领域层已保证取值为 `None`）
- [x] 界面/展示按 `BindingForm` 渲染，不靠"取不到值"判断形态（提供 `is_git` / `is_plain` 显式判定）
- [x] **`plain` 项目可正常建立**：目录不是 Git 仓库不拒绝建立、不降级检查范围、不显示空仓库字段
- [x] `Dependency` 允许循环（`a→b→a` 合法），循环不阻止建立项目
- [x] `Dependency` 拒绝自环；边方向为**使用者 → 提供者**
- [x] `DependencyOrigin.INFERRED` 时 `source` 非空（FR03 需标注"推导结果，可能不完整"）
- [x] 依赖边为**唯一权威来源**，`Module` 上无第二处可写的依赖字段（见设计说明第 4 节决定 1）
- [x] `EnvironmentRef` 不含已解析事实（`interpreter_identity`、`dependency_set_digest`）；声明与解析结果分离
- [x] `IsolationMode` 为三态枚举；`venv` 之外必须有显式确认；**显式不隔离是合法事实**，未自动降低证据等级
- [x] `SecretRef` 在类型层面**无法表达凭据正文**（用 `dataclasses.fields` 断言字段集合，防止后续新增 `value` 类字段）
- [x] `SecretRef.purpose` 必填（模型 / HTTP / 数据库核验 / GitHub 分用途）
- [ ] 环境缺失载体**阻塞**（**待 Sprint 2 应用用例**；领域层已记录隔离方式与 `data_isolated=None` 表示未知）
- [x] `data_isolated` 未知记 `None`，未用 `False` 冒充"未隔离"
- [x] `Delivery` 的自述与验证事实**结构分离**：`self_report` 与 `verified_in_scope` 是不相交的字段
- [x] 自述称已完成**不能**使 `verified_in_scope` 非空（有对应测试）
- [x] `SourceManifest` 的 `content_digest` 必填；`mtime_hint` 仅作变化提示，无"仅比 mtime 即判相同"的代码路径
- [x] 清单内路径为**相对路径**且唯一，无盘符、无 `..`
- [x] 复取依赖与可复取范围**显式登记**；缺失即为缺口，不留空冒充完整
- [x] 同步盘/网络盘位置**拒绝建立工作空间**并**说明原因**（返回 `WorkspaceLocationRejection`，不只返回布尔）
- [x] `DriveKind.UNKNOWN` 未被领域层擅自判为拒绝（由调用方决定是否阻塞）

## 3 模板与草稿（FR04）

- [ ] 模板只生成草稿，**未提供自动发布路径**
- [ ] 模板给出：适用条件、稳定 `item_id`、必测集合、关键链路清单
- [ ] 空关键链路有 `no_critical_path_reason`
- [ ] 模板正文未写成可执行 shell
- [ ] 未在项目上下文缺失时编造依赖与结论，而是列缺口并阻塞
- [ ] 人工修订不会被重新生成覆盖
- [ ] `GeneratedContent` 记录了**实际依赖的修订集合**
- [ ] 相关来源变化后草稿被标**定向**过期（未变部分可复用）
- [ ] 六个模板的真实版本可在安装后被列出

## 4 规则版本（FR05）

- [ ] 规则正文、结构化规则、发布状态分开保存
- [ ] 导入只创建草稿，不执行其中命令
- [ ] 未确认发布不生效
- [ ] 发布后不可变；新测试绑定新修订，旧运行仍引用原版
- [ ] 往返保留编号、来源与未识别扩展字段
- [ ] **未知执行字段不得静默启用**

## 5 用例、依据与计划（FR06）

- [ ] 用例含：层级、前置、输入、步骤、预期、验证方法、允许 Mock 范围、重要性
- [ ] 无预期/前置/必要核验方式的用例不能发布为可直接验收
- [ ] 发布时关联**全部适用关键链路**并登记**独立核验方式**
- [ ] 确无适用链路时保存显式空集合**及理由**；缺清单字段仍阻止发布
- [ ] `missing` 依据不能进入 full 必测
- [ ] `present_unconfirmed` 可执行、保留分母、计"断言未核实"、**不计 V**
- [ ] `confirmed` 仍须实际证据/独立核验有效才计 V
- [ ] `assertion_basis_state` 是冻结值；`effective_assertion_basis_state` 是**派生值**
- [ ] 依据文本变化后**旧确认失效**
- [ ] 补充确认**没有**直接把 `verified_count` 加一
- [ ] `CaseLink` 覆盖验收项/模块/检查内容/适用环境
- [ ] `AcceptanceScope` 含目标、包含/排除及原因、依赖闭包、适用性、模板修订
- [ ] 计划发布固定**精确**的用例与规则修订（不是"最新"）

## 6 档位、范围与门禁（FR07）

- [ ] `T ⊆ M`；`full` 要求 `M ⊆ S`
- [ ] `full` 跳过项为 0
- [ ] `ConclusionCeiling` **只由档位派生**，未由覆盖率或驱动方式放宽
- [ ] `quick` / `on_demand` 的结论上限为 `partial`，证据等级为"不打分"
- [ ] 不适用项给出原因且**不计入分母**
- [ ] "未执行"未被当作"不适用"
- [ ] 运行中不得删必测、弱化断言、变更适用性；违反时拒绝或要求新建运行
- [ ] 无关新增用例**不能**抵消模板漏项
- [ ] 驱动只允许 `planned → stepwise`；反向被拒并提示新建运行
- [ ] 运行中修订只作用于**尚未执行**的步骤
- [ ] 正在执行的步骤拒绝修改
- [ ] 依据失效时暂停并列出需重跑的用例，当前完整通过**不使用失效依据**

## 7 准备意图与幂等

- [ ] 源码读取与摘要计算在**短事务外**
- [ ] 按 `(project_id, client_id, prepare_request_id)` 登记 `PreparationRecord`
- [ ] 同请求同输入**幂等**返回；同键异输入摘要返回**冲突**
- [ ] 并发完成同一准备请求**只发布一条**记录，其余取得已发布结果
- [ ] `intent_id` 与准备记录**同一次提交**
- [ ] 未用 `request_id` 代替业务身份
- [ ] prepare 期间源码变化 → 返回"依据需重新准备"，**未把新字节塞进旧意图**
- [ ] 提交时校验项目、绑定、计划、规则、模板、环境修订及准备请求是否仍有效
- [ ] `expected_revision` 冲突时返回当前修订与差异提示，**不自动覆盖用户编辑**

## 8 源码与环境

- [ ] 内容身份来自**实际字节摘要**，未用 mtime 证明相同
- [ ] 元数据仅作为变化提示
- [ ] 记录仓库/LFS/子模块等**复取依赖**与可复取范围；缺失时明确缺口
- [ ] 未保存缓冲区先由用户保存或明确选择磁盘版本，决定入记录
- [ ] 环境记录：隔离方式、运行时与版本、依赖集合摘要、超时、数据来源/隔离/复位、目标部署身份、按用途 `SecretRef`
- [ ] 显式不隔离被作为**合法事实**记录，未自动降低证据等级

## 9 模型出站与 GitHub

- [ ] `ModelOutboundPolicy` 按项目修订保存，含接收地址/供应方、允许类别、`source_snippets=false` 默认值
- [ ] 只发送**显式选定的脱敏材料**；源码片段开关作用于所有出站字段与日志/附件投影
- [ ] 未通过另一类别夹带未授权源码
- [ ] 模型只输出草稿/假设，未直接改断言、关闭缺陷或作结论
- [ ] 迟到响应核对来源修订并标过期，**未覆盖当前人工版本**
- [ ] 模型不可用时人工路径仍可用；**未在失败重试中暗换供应商**
- [ ] GitHub 只读为可选；plain 未注册、未调用 Git 能力
- [ ] 未把 GitHub 绿色状态当作 L1 通过或业务通过
- [ ] 凭据正文未进入配置、日志、面板、导出

## 10 测试与交付

**Sprint 1 状态（2026-09-25）**：依赖一致性、静态检查、类型检查、全量测试与 Schema 一致性已按 CI 相同调用方式执行；证据见第 11 节。

- [x] 领域单元测试覆盖合法**与非法**状态
- [x] 合同测试覆盖成功/失败/未知三类 `PreparedRun` 夹具（Sprint 0 已通过）
- [x] 词汇表漂移锁定测试通过（domain ↔ contracts）：`test_run_vocabulary.py`、`test_project_vocabulary.py`
- [x] 架构依赖方向测试通过
- [x] Schema 由脚本生成，**未手工编辑**（Sprint 1 未改动 `contracts/`）
- [x] `uv sync --extra dev --locked` 通过（21 个包与 `uv.lock` 一致）
- [x] `uv run ruff check .` 通过
- [x] `uv run mypy` 通过（strict，95 个源文件）
- [x] `uv run pytest -q` 通过（111 项；`tests/recovery/` 2 项受开发环境系统临时目录写权限限制，见第 11 节）
- [x] `git diff --exit-code -- src/aitest/contracts/schemas` 为空
- [x] 修改日志已更新（`docs/修改日志/feix-a/2026-09-25-B包项目上下文领域层.md`）
- [x] 交付说明分**四类**写（已实现 / 静态检查通过 / 真实环境验收 / 未验证）
- [x] 未将未完成的动作注册为可用能力（`interfaces/local/api.py` 未改动）
- [x] 未更新非 B 牵头的 AC 条目

---

## 11 Sprint 1 验证证据

执行环境：Windows 11 x64 · Python 3.13.3 · uv 0.12.19 · 分支 `feat/package-b-project-planning`
调用方式与 CI 一致：`py -3.13 -m uv run <工具>`；uv 缓存目录 `UV_CACHE_DIR` 须指向工作区内目录。

| 检查 | 命令 | 结果 |
| --- | --- | --- |
| 依赖一致性 | `uv sync --extra dev --locked` | 通过（21 个包与 `uv.lock` 一致） |
| 静态检查 | `uv run ruff check .` | 通过（All checks passed） |
| 类型检查 | `uv run mypy`（strict） | 通过（no issues in 95 source files） |
| 全量测试 | `uv run pytest -q` | **111 passed, 2 errors** |
| Schema 一致性 | `git diff --exit-code -- src/aitest/contracts/schemas` | 无差异 |

**2 项 error 说明**（与本次改动无关）：

```
ERROR tests/recovery/test_atomic_and_lock.py::test_failed_publish_preserves_old_pointer
ERROR tests/recovery/test_atomic_and_lock.py::test_real_second_process_cannot_acquire_writer_lock
E   PermissionError: [WinError 5] 拒绝访问:
    'C:\Users\LENOVO\AppData\Local\Temp\dsh-*\pytest-of-LENOVO'
```

该两项使用 pytest `tmp_path` 夹具，当前开发环境不允许写系统临时目录。
Sprint 1 改动前后报错项与数量一致。**该两项在开发环境未验证，须在常规开发终端复跑确认。**

### 11.1 本次新增／修改的测试

| 文件 | 覆盖内容 |
| --- | --- |
| `tests/unit/test_bindings.py`（新增） | `git` 三字段必填；`plain` 必须省略三者且必须有清单摘要；`plain` 是一等形态；相对路径与 `..` 拒绝；移动产生新修订且旧修订保留 |
| `tests/unit/test_modules_and_dependencies.py`（新增） | 模块必填字段与输入输出；自环拒绝；`inferred` 必须有来源；图引用完整性；**循环 `a→b→a` 合法且传播去重**；无依赖时仍保留变更模块 |
| `tests/unit/test_environments.py`（新增） | 默认 `venv`；显式不隔离是合法事实；非默认隔离必须确认；`data_isolated` 未知记 `None`；超时为正；网络目标与凭据键唯一；**`SecretRef` 字段集合锁定为 3 个、类型层面无法承载凭据正文**；`EnvironmentRef` 不含已解析事实 |
| `tests/unit/test_workspace_location.py`（新增） | 网络盘／同步盘拒绝且带原因；固定盘与可移动盘放行；`UNKNOWN` 不由领域层擅自拒绝 |
| `tests/unit/test_source_manifest.py`（新增） | 内容摘要必填；相对路径唯一且限定在范围内；`size` 非负；`mtime` 仅为可选提示；复取依赖与范围显式登记；缺失即为缺口 |
| `tests/contracts/test_project_vocabulary.py`（新增） | `domain.BindingForm` 与 `contracts.BindingFormFact` 值集合与成员名一致 |
| `tests/unit/test_projects.py`（修改） | 原"模块 id 唯一"断言保留；新增身份别名、跨项目模块拒绝、必填字段与修订下限、未知模块查询 |
| `tests/unit/test_deliveries.py`（修改） | 原"自述重叠拒绝""`run_method` 必填"断言保留（重叠项改为对 `SelfReport` 断言）；新增**自述完成不等于验证完成**、已验证范围需执行事实、已验证与未验证不相交、修订递增 |

> **`Delivery` 的构造参数有破坏性变更**：`completed` / `incomplete` 由顶层字段移入 `self_report`
> （需求 P1-FR02 要求开发自述与测试验证分别展示，原结构无法区分两者）。
> 二者保留为**只读属性**，读取方无需改动；直接以这两个名字构造的地方需改为 `self_report=SelfReport(...)`。
> `Project` 类由 `LocalProject` 取代并删除（`Project` 无生产代码引用，保留会形成两套项目身份）。

### 11.2 尚未验证（不得据此声称一期验收通过）

- `tests/recovery/` 2 项在开发环境的通过结果（受系统临时目录写权限限制）；
- 其余一切真实环境验收：本 Sprint 产物是纯领域规则，**无端到端运行结果**；
- `git` 形态源码身份、`SourceSnapshot` 归属（待 A 或组长裁定）；
- 序列化层"真正省略不适用键"（属 Sprint 2）。
