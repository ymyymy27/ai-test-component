# 三期架构总览：只上传

版本：3.4  
更新日期：2026年9月23日  
文档类型：工程设计文档

范围仅P3-FR01账号关联和P3-FR02结果上传，P3-AC01—08。平台提供账号/参与项目/接收与回执；插件实现会话使用、冻结投影、候选/提交、查询/补缺与本地状态。

| 层 | 增量 |
| --- | --- |
| 应用 | collaboration/account_session、result_delivery |
| 领域/数据 | identity/accounts、delivery/submissions；与测试判定分离 |
| 适配器 | AccountPort与ResultSubmitterPort出站实现 |
| 凭据 | 按账号增加令牌引用命名空间 |
| 视图 | 账号/项目设置和本账号提交队列 |
| 存储 | 沿用工作单元，新增候选游标、不可变提交/回执、脱敏产物引用 |

不实现接收用例、共享查询、跨账号审核、ASGI宿主、HTTP服务路由或远程MCP。历史参考中的这些模块不进入插件依赖图或验收。

本地核心可选加载上传模块，不经过平台宿主。平台依赖导入/连接失败仅标上传不可用；发送请求在事务外、独立于执行通道，候选由本地提交游标恢复，防止提交成功后尚未入队时崩溃漏结果。

先完成[账号合同](01-账号与项目关联.md)与[上传合同](02-结果上传.md)，平台接口四项前置明确后实现和对拍。平台不可用时一期/二期原功能全部保持；该要求不表示存储本身不可写时仍可运行。

## 对接与隔离验证

| 责任 | 插件完成内容 | 平台前置 |
| --- | --- | --- |
| 身份与参与 | 会话使用、精确账号引用、绑定/策略确认 | 会话取得/刷新、账号/项目标识及参与检查 |
| 已保存结果传输 | 投影、冻结清单、候选/游标、发送/查询/补交 | 提交方式、限制与错误语义、原号查询 |
| 接收确认 | 逐对象摘要/修订/归属核对及本地状态 | 完整回执和缺对象表达 |
| 平台不可用 | 只降上传、保留材料、本地继续 | 不要求平台在线才能启动本地核心 |

验证平台SDK缺失/导入失败、会话过期、接口未配置、断网、回执冲突和队列压力时的本地闭环。会话与上传模块可选装配，失败不能连带阻断一期模块导入。三期八项AC只检验插件账号关联与上传行为，平台内部存储/审核/宿主不纳入本地工程。

### 范围

| 归属 | 分册 | 覆盖功能 | 内容 |
| --- | --- | --- | --- |
| **插件** | [01 账号与项目关联](01-账号与项目关联.md) | 三期 P3-FR01 | 会话使用（平台提供）、项目与目标选择、上传核对时点 |
| **插件** | [02 结果上传](02-结果上传.md) | 三期 P3-FR02 | 候选、冻结投影、送达许可、分块进展、幂等回执与有界补交 |
| 平台侧 | 平台外部接口 | — | **不在插件范围**：平台内部部署、接收、审核和共享查询由平台负责 |

验收场景为三期 P3-AC01—P3-AC08，共 8 项，**全部为插件侧**（上传与本地不受影响），见[三期功能文档](../功能文档/01-功能文档.md)第1节。

### 平台职责与插件职责

| 事项 | 平台 | 插件 |
| --- | --- | --- |
| 账号、项目编号、成员关系 | 提供并维护 | 只读取，不维护第二套 |
| 项目与仓库关联 | 提供并维护 | 只读取并选择 |
| 接收结果、去重、回执 | 提供接口与存储 | 按协议提交、查询、补交 |
| 会话校验与身份认定 | 校验会话并给出稳定账号 | 使用返回的账号；正文自填不作身份依据 |
| 本地测试与记录 | 不涉及 | 完全由插件负责，平台失联时不受影响 |

### 与一期、二期的关系

| 主题 | 处理方式 |
| --- | --- |
| 三层判定、统计分母、结论上限 | **不变**；上传不改变判定规则与结论 |
| 记录不可变与修订机制 | **不变**；上传引用精确修订与清单摘要 |
| 本地项目编号与来源 | **保留为来源信息**，登录后不回写为平台账号 |
| 落盘与恢复 | **沿用**一期工作单元；`outbox` 与回执恢复使用同一提交机制 |
| 编辑器入口与降级 | **沿用**二期；三期只增加"上传"这一类本地用例，不增加宿主与远程入口 |

### 完成标志

**上传实现的前置条件**
- 会话获取与刷新方式、提交接口形态（字段/大小限制/错误码）、回执语义、"账号 + 项目"标识口径四项确定后即可实现上传；未确定也不阻塞本地能力。

**共同底线**
- 平台不可用时，一期 P1-AC01—P1-AC35、二期 P2-AC01—P2-AC09 的本地能力仍全部可用。
- **降级可验证**：平台接入不可用（可选模块未加载、依赖缺失或平台连接失败）时，`aitest doctor` 仍返回本地就绪；协作能力标为不可用并给出原因，本地面板、CLI 与执行不受影响。
