# 2026-09-25 C包Spool与串行闭环

## 本次完成

- 增加CapturedOutputBlock和SpoolManifest领域对象。
- 增加SpoolStore端口。
- 实现FileSpoolStore。
- 只允许已封口块落盘。
- 实现块文件、摘要、长度、归属和清单校验。
- 实现不可变块冲突检测和目录穿越拒绝。
- 实现确定性FakeExecutionPort和FakeExecutionSpec。
- 扩展SerialRunner为持续轮询、采集和状态回写的串行循环。
- 集成SpoolStore，保证块保存成功后才更新Attempt引用。
- 实现上游execution_error阻塞下游。
- 增加Spool、假适配器和两步依赖闭环测试。
- 将structured_result_ref同步到ExecutionFacts合同并重新生成Schema。
- 记录Mock适配器与最终包装器spool写入路径的差异。

## 验证

- pytest：55 passed。
- ruff check：通过。
- ruff format --check：152个文件符合格式。
- mypy strict：97个源文件无问题。

## 未完成

- 对象库发布和正式EvidenceRef认领。
- WorkspaceUnitOfWork持久化。
- 检查点、恢复扫描和崩溃续跑。
- 真实命令包装器与进程组。
- 多流独立游标。
