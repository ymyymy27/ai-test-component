# 2026-09-26 B-C 契约对齐

## 本次完成

- 从最新develop切出独立契约分支feature/contract-b-c-preparedrun-execution-facts。
- 将ExecutionFacts合同、Schema、夹具和合同测试带入契约分支。
- 删除C侧重复的RunTierFact。
- 从aitest.contracts.prepared_run导入RunTierFact、RunDriverFact和ConclusionCeilingFact。
- RunFact.driver和RunFact.conclusion_ceiling改为B合同枚举。
- RunFact增加非空intent_id。
- success、failure、unknown夹具的conclusion_ceiling改为passable。
- 新增quick.json，覆盖quick档的partial结论上限和空证据等级。
- required_scope和selected_scope补充M与S来源说明。
- 增加顶层plan_revision与RunFact.plan_revision一致性校验。
- 在B-C公共对接文档追加C确认结论和确认记录。
- 更新接口对接索引中的B-C合同状态。
- 重新生成ExecutionFacts JSON Schema。

## 验证

- pytest：122 passed。
- ruff check：通过。
- 本次C修改文件ruff format：通过。
- mypy strict：96个源文件无问题。
- quick夹具和plan_revision拒绝分支已由合同测试覆盖。

## 说明

- develop基线中B所属Markdown代码块和prepared_run.py存在ruff format差异，本PR未越权修改B文件。
- 本分支只处理B-C契约对齐，不包含runner、spool和假适配器实现。
- 公共文档B侧签署栏保持未勾选，等待B在契约PR Review中确认。
