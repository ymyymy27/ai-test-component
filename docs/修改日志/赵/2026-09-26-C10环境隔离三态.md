# 2026-09-26 C-10 环境隔离三态

## 本次完成

- 从 develop 创建 feature/contract-b-c-environment-isolation-mode。
- 从 prepared_run.py 导入 EnvironmentIsolationModeFact。
- RunFact.environment_isolated 改为 environment_isolation_mode。
- success、failure 夹具使用 venv。
- unknown 夹具使用 none。
- quick 夹具使用 unmanaged。
- 重新生成 ExecutionFacts JSON Schema。
- 合同测试覆盖四种夹具和三态取值。
- B-C 公共文档追加第 7.4 节，版本更新为 0.7。
- 更新接口对接索引状态。

## 验证

- pytest：126 passed。
- ruff check：通过。
- 本次修改文件ruff format检查：通过。
- mypy strict：96个源文件无问题。
- 四份夹具分别覆盖venv、venv、none、unmanaged。
- ExecutionFacts Schema引用B合同EnvironmentIsolationModeFact，旧environment_isolated字段已移除。

## 说明

- 本次只做 C 侧合同与夹具同步，不包含 runner、spool 或真实适配器实现。
- fetch、push 和 PR 由赵本人执行。
