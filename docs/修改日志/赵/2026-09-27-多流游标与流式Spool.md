# 2026-09-27 多流游标与流式Spool

## 本次完成

- Attempt、CollectionResult、Checkpoint和SpoolManifest改为多流游标集合。
- ExecutionFacts AttemptFact改为output_cursors数组。
- success合同夹具覆盖stdout和stderr独立游标。
- CommandAdapter加入timeout_ms监督和timed_out记录。
- ExitFact增加timed_out，并增加timeout终止原因。
- SerialRunner把超时Attempt置为pending_verification。
- Windows增加Job Object和taskkill进程树回收。
- POSIX增加killpg进程组回收。
- FileSpoolStore支持真正的流式append写盘。
- CommandAdapter在进程运行期间持续过滤并封口块。
- 增加流式写盘、双流游标、超时和子进程组回收测试。

## 验证

- pytest：160 passed。
- ruff check：通过。
- 本次修改文件ruff format检查：通过。
- mypy strict：99个源文件无问题。

## 未完成

- 崩溃后未封口尾部恢复。
- 对象库发布和正式EvidenceRef认领。
- 检查点持久化。
