# AI 辅助测试插件开发协作指南

本文件适用于参与本项目需求、核心、适配器、界面、测试和文档开发的所有人员与自动化开发工具。它提供共同工作规则；字段、状态、时序和验收预期仍以对应的现行分册为准。当前目录是**文档工作区**，不是产品代码仓库；[目标工程结构](docs/总体架构.md#11-目标工程结构)描述拟实现的路径，不能据此声称代码或测试已经存在。

## 1. 从哪里读取合同

1. 先读[文档总览](docs/README.md)、[总体架构](docs/总体架构.md)和[阅读索引](docs/阅读索引.md)，再按任务所在期次读需求、功能、架构分册和界面设计。
2. **需求文档**确定范围、FR 和 AC 验收预期；**功能文档**确定输入、操作、输出与业务规则；**架构文档**确定模块、字段、接口、提交与恢复合同；**设计文档**确定页面动作和呈现。跨册同一字段、枚举或公式不得另起定义。[阅读索引第 4、5 节](docs/阅读索引.md#4-合同实施入口)列出主责位置及同步维护点。
3. [完整合订修订稿](工程设计文档完整修订版-2026-09-23.md)便于通读，条款已回写到对应分册；日常维护仍在原分册。历史评审报告、整改记录、打包副本和架构图用于追溯，不作为覆盖现行分册的平行规范。发现现行分册之间冲突时，记录涉及的 FR/AC、两处原文及影响，先核对阅读索引指定的主责合同，再同步修正受影响的需求、功能、架构、界面与测试；不能只在代码中选一种解释。
4. 现行编号基线为一期 **17 FR / 35 AC**、二期 **6 FR / 9 AC**、三期 **2 FR / 8 AC**，合计 **25 FR / 52 AC**。增删、改义或跨期移动须显式更新映射和验收范围，不通过重编号掩盖变化。

## 2. 期次和交付边界

| 期次 | 开发目标 | 完成证明 |
| --- | --- | --- |
| 一期 | Windows 11 x64、Python 3.13、Trae 上的本地测试闭环：项目/计划、L1/L2/L3、证据、判定、报告、问题、恢复、面板、CLI 与 stdio MCP | P1-AC01—35 的真实输入、版本和证据；真实 Trae 接入与故障恢复 |
| 二期 | 复用同一核心，增加非 Trae 编辑器、操作系统组合、源码定位、依赖/链路图、能力诊断与面板增量 | P2-AC01—09 的证据；至少一个新增编辑器和一个新增系统组合实测 |
| 三期 | 使用平台提供的账号与项目身份，将已保存结果安全投影、冻结、出站发送并核对回执 | P3-AC01—08 的插件侧证据；平台合同确定后进行字节级对拍和中断恢复 |

一期的内部里程碑不等于一期交付；不能省略人工证据、外部导入、运行中修订或恢复场景。二期不重算一期的判定与统计。三期不实现平台接收、审核、共享查询或宿主，不引入本地 HTTP 服务、数据库、消息队列或独立发送 Worker；平台不可用时，已具备的本地能力仍应可用。具体范围见[各期架构总览](docs/一期/架构文档/00-架构总览.md)、[二期总览](docs/二期/架构文档/00-架构总览.md)及[三期总览](docs/三期/架构文档/00-架构总览.md)。

## 3. 实现结构与职责

- 保持**入口 → 接口适配 → 应用用例 → 纯领域规则**的依赖方向。`application/ports.py` 是外部能力端口的归属，`infrastructure/` 实现端口，`bootstrap.py` 注入实现；应用用例不直接导入基础设施。按实际需要增加模块，不预建空服务或为每条记录复制一套服务层。
- 判定、覆盖、证据等级、复用资格和问题关闭依据只在领域规则中计算一次。Trae、其他编辑器、CLI、MCP、面板、报告与上传只调用同一核心或读取其 DTO；宿主和面板不得直接写业务文件或重算结论。共享面板只有一份源码，构建产物和生成 Schema 不手工编辑。
- 一个用户数据工作空间由一个核心进程持排他写锁。业务变更经应用用例确定短事务边界，由工作单元提交不可变记录、引用、索引和幂等结果；外部执行、模型与网络调用在事务外。跨入口重传同一持久业务意图不得重复执行；明确重跑须创建新意图。发布结果前确认材料已可靠保存。
- 运行数据位于操作系统用户数据工作空间，不放进安装包、被测源码或 `src/`。业务记录、证据、报告修订、导出和诊断永久保留；只按[存储与恢复合同](docs/一期/架构文档/04-存储与恢复.md)回收已确认可回收的临时材料。格式迁移先做可校验备份，恢复先核实活动执行与可抢救输出；未知副作用不自动重放。
- 源码绑定、实际入口/解释器/加载来源和运行环境必须能核对；源码物化不等于环境或远端一致。执行、导出及展示使用同一来源事实，来源无法证实时保留“待核实”或阻塞状态。[执行来源合同](docs/一期/架构文档/01-项目与计划.md#12-执行来源与物化边界)是主责位置。
- 代码、项目文档、日志和模型输出均作为不可信输入处理。已知凭据在**落盘前**过滤，过滤前字节不得写入 spool、对象、诊断或备份；无法安全处理的材料登记缺口，不伪造完整证据。凭据正文不进配置、日志、面板、导出或上传。[采集安全合同](docs/一期/架构文档/02-执行与证据.md#13-采集安全与原始证据语义)与[上传白名单](docs/三期/架构文档/02-结果上传.md#2-投影与摘要)分别约束本地采集和出站材料。

## 4. 不可绕过的业务边界

- 区分业务结论 `passed / failed / incomplete / not_applicable`、证据等级、执行完成与有效验证。只有 `full` 计算证据等级；A 级不等于业务通过。`quick` 和 `on_demand` 不升级为完整通过；空分母不显示 100%。`M` 是冻结必测范围，`S` 是本轮所选范围，`E` 是本轮有效整用例执行，`R` 是合法整用例复用，`V` 是有效验证；`E∩R=∅`，`V⊆E∪R⊆S`，`full` 要求 `M⊆S`。有效决定性失败优先按领域合同派生，未完成范围仍须展示。[唯一判定口径](docs/一期/功能文档/01-功能文档.md#3-判定与统计唯一口径)是主责位置。
- Attempt 属于 Step。新 Attempt 会撤销整用例旧复用资格，并使实际依赖旧上游结果的下游依据过期；历史事实保留。不得把跨运行继承与本轮部分执行拼成整用例 `E/R/V`，也不能用复用代替缺陷关闭所需的新执行。[聚合与失效合同](docs/一期/功能文档/01-功能文档.md#15-用例执行聚合与依赖失效)是主责位置。
- 发布计划、动作授权、断言依据确认、实际执行和独立核验是不同事实。人工确认绑定准确的依据修订，不回写冻结计划或用例；补确认不伪造执行或核验。真实结果未知、证据缺失、依赖过期和未测试都应如实显示。
- 二期源码定位须区分历史快照与当前编辑器内容；dirty 缓冲区不能得到 `current_verified`，也不自动保存。宿主缺校验能力时降级为快照或位置文字；按[六态定位合同](docs/二期/架构文档/02-源码定位与行内标注.md#1-用例与状态)处理未知值。图只是已有事实的投影，不产生新判定。
- 三期接收事实、暂停/放弃控制和单份 `SubmissionDeliveryGrant` 分开保存。退出、换账号、暂停或关闭同步撤销许可但保留材料；只有明确放弃才进入 `ABANDONED`。失联先查原提交号，不能盲发或重新执行业务；安全预览不等于发送授权。冻结投影、清单规范字节、摘要与平台回执须逐项核对。[上传合同](docs/三期/架构文档/02-结果上传.md)是主责位置。
- 列表、报告、历史和队列使用[有限 QuerySpec 与索引合同](docs/一期/架构文档/04-存储与恢复.md#13-有限查询与索引键)；列表读摘要，详情按引用读取。能力降级按**具体动作**的真实依赖判断，不把某个适配器故障扩散为全部核心能力故障。

## 5. 每项工作的实施顺序

1. 明确期次、FR/AC、主责分册、输入/输出、异常分支及受影响的记录/DTO/界面；检查工作区已有改动，保留他人的未提交成果。
2. 先实现领域规则和稳定身份，再实现应用用例、端口适配、入口与界面；涉及持久化时同时设计幂等、断点恢复、旧记录读取和安全投影。新增动作按版本/能力协商暴露，保持已发布字段语义兼容。
3. 为有实际风险的边界写验证：规则夹具、合同/schema、架构依赖、提交/恢复故障注入、真实宿主/系统/平台集成。按 AC 编号记录输入、软件及制品版本、预期、实际、证据位置和未验证原因。断言不能只检查“调用成功”或界面提示。
4. 修改规范时在原文基础上精确修订，保留仍有效的内容与历史；同步需求、功能、架构、设计、FR/AC 映射及测试夹具。较大修订先留可恢复备份和逐项变更记录，再检查链接、锚点、表格、编号、字段/枚举、状态迁移和主责语义；不要手改生成物。
5. 交付说明分别列出**已实现、已通过静态/单元/合同检查、已通过真实环境验收、未验证**。文档一致、包可构建或模拟器通过，均不能写成真实 Trae、其他宿主、掉电恢复或平台对拍通过。

产品完成标准及测试目录见[总体架构第 8 节](docs/总体架构.md#8-工程与完成标准)和[工程与验证映射](docs/总体架构.md#14-功能到工程模块与验证)。具体命令以实际代码仓库的工程配置和锁文件为准；本文件不预设尚未创建的 CI 命令或已经通过的验收结果。
